'use client'

import { type Dispatch, type SetStateAction } from 'react'

type Choice = [string, string, string]
const choices: Choice[] = [['💧', 'Stay hydrated', 'Drink water'], ['🍽️', 'Eat properly', 'Eat breakfast'], ['📖', 'Read', 'Read a book'], ['🏃', 'Move your body', 'Move your body'], ['❤️', 'Show love', 'Express appreciation'], ['🌿', 'Take time for yourself', 'Relax / self-care']]
export function Onboarding({ step, setStep, selected, setSelected, finish }: { step: number; setStep: Dispatch<SetStateAction<number>>; selected: string[]; setSelected: Dispatch<SetStateAction<string[]>>; finish: () => void }) { return <div className="fixed inset-0 z-50 flex items-center justify-center bg-[#fbfaf8] p-6 dark:bg-[#151617]"><div className="w-full max-w-xl text-center">{step === 1 && <><div className="mx-auto flex size-20 items-center justify-center rounded-[2rem] bg-gradient-to-br from-violet-500 to-fuchsia-400 text-4xl">🌱</div><h1 className="mt-8 text-4xl font-semibold md:text-6xl">Welcome to <span className="font-serif italic text-violet-500">LittleWins</span></h1><p className="mt-5 text-lg text-muted-foreground">Build better days, one small win at a time.</p><button onClick={() => setStep(2)} className="mt-9 rounded-full bg-foreground px-7 py-4 font-semibold text-background">Get started</button></>}{step === 2 && <><h1 className="text-4xl font-semibold">Choose what matters to you.</h1><div className="mt-8 grid grid-cols-2 gap-3 text-left">{choices.map(([icon, label, title]) => <button key={title} onClick={() => setSelected(current => current.includes(title) ? current.filter(item => item !== title) : [...current, title])} className={`rounded-2xl border p-4 ${selected.includes(title) ? 'border-violet-400 bg-violet-50 dark:bg-violet-950/40' : 'border-border bg-background'}`}><span className="text-2xl">{icon}</span><p className="mt-3 text-sm font-semibold">{label}</p></button>)}</div><button onClick={() => setStep(3)} className="mt-8 rounded-full bg-foreground px-7 py-4 font-semibold text-background">Continue</button></>}{step === 3 && <><div className="text-6xl">✨</div><h1 className="mt-6 text-4xl font-semibold">Your day, your goals.</h1><p className="mt-4 text-lg text-muted-foreground">Customize every objective as you grow.</p><button onClick={finish} className="mt-9 rounded-full bg-foreground px-7 py-4 font-semibold text-background">Start my journey</button></>}</div></div> }
