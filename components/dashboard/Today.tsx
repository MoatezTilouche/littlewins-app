'use client'

import { Heart, Plus, Trophy } from 'lucide-react'
import { motion } from 'framer-motion'
import { greeting, type Objective } from '@/lib/db'
import { EmptyState, ObjectiveCard, ProgressRing } from './shared'

export function Today({ active, percent, completed, getValue, updateProgress, onAdd, nickname, streak }: { active: Objective[]; percent: number; completed: number; getValue: (objective: Objective) => number; updateProgress: (objective: Objective, value: number) => void; onAdd: () => void; nickname: string; streak: number }) { const why = active.find(objective => objective.why)?.why; return <div><div className="mb-8 flex flex-col justify-between gap-6 md:flex-row md:items-end"><div><p className="mb-2 font-medium text-violet-600">{greeting()}, {nickname} ✦</p><h1 className="text-4xl font-semibold tracking-tight md:text-5xl">Make today <span className="font-serif italic text-violet-500">count.</span></h1><p className="mt-3 max-w-md text-muted-foreground">A few gentle intentions can change the feeling of a whole day.</p></div><button onClick={onAdd} className="flex w-fit items-center gap-2 rounded-full bg-foreground px-5 py-3 text-sm font-semibold text-background shadow-lg"><Plus className="size-4" />Add objective</button></div><section className="mb-8 grid gap-5 md:grid-cols-[1.4fr_.6fr]"><div className="relative overflow-hidden rounded-[2rem] bg-gradient-to-br from-violet-500 via-purple-500 to-fuchsia-500 p-6 text-white md:p-8"><div className="relative z-10 flex items-center justify-between"><div><p className="text-sm text-white/75">Today’s progress</p><p className="mt-2 text-5xl font-semibold">{percent}<span className="text-2xl">%</span></p><p className="mt-2 text-sm text-white/75">{completed} of {active.length} little wins complete</p></div><ProgressRing value={percent} /></div></div><div className="flex flex-col justify-between rounded-[2rem] border border-border/60 bg-background p-6"><div className="flex items-center justify-between"><span className="text-3xl">🔥</span><span className="rounded-full bg-amber-100 px-3 py-1 text-xs font-bold text-amber-700 dark:bg-amber-950 dark:text-amber-300">{streak} day streak</span></div><div><p className="text-sm text-muted-foreground">Keep your rhythm</p><p className="mt-1 text-lg font-semibold">{percent === 100 ? 'You completed every little win.' : percent > 0 ? 'Every small step is adding up.' : 'Today is a fresh opportunity.'}</p></div></div></section>{why && <div className="mb-8 flex items-start gap-4 rounded-3xl border border-violet-200/70 bg-violet-50/70 p-5 dark:border-violet-900 dark:bg-violet-950/30"><Heart className="size-5 text-violet-500" /><div><p className="text-xs font-bold uppercase tracking-widest text-violet-600">Remember why you started</p><p className="mt-1 font-serif text-lg italic">“{why}”</p></div></div>}<div className="mb-4 flex items-center justify-between"><h2 className="text-xl font-semibold">Your little wins</h2></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{active.map(objective => <ObjectiveCard key={objective.id} objective={objective} value={getValue(objective)} update={updateProgress} />)}</div>{!active.length && <EmptyState onAdd={onAdd} />}{percent === 100 && active.length > 0 && <motion.div initial={{ scale: 0.95, opacity: 0 }} animate={{ scale: 1, opacity: 1 }} className="mt-6 rounded-[2rem] bg-gradient-to-r from-amber-100 to-pink-100 p-8 text-center dark:from-amber-950/40 dark:to-pink-950/30"><Trophy className="mx-auto mb-3 text-amber-500" /><h3 className="text-2xl font-semibold">You did it!</h3><p className="mt-2 text-muted-foreground">Today you kept the promises you made to yourself.</p></motion.div>}</div> }
