'use client'

import { type Dispatch, type FormEvent, type SetStateAction } from 'react'
import { motion } from 'framer-motion'
import { X } from 'lucide-react'
import { type Category, type Objective, type ObjectiveType } from '@/lib/db'

type ObjectiveFormState = { title: string; icon: string; category: Category; type: ObjectiveType; target: number; unit: string; frequency: string; why: string }
export function ObjectiveForm({ form, setForm, editing, onClose, onSave }: { form: ObjectiveFormState; setForm: Dispatch<SetStateAction<ObjectiveFormState>>; editing: Objective | null; onClose: () => void; onSave: (event: FormEvent) => void }) { const update = (patch: Partial<ObjectiveFormState>) => setForm(current => ({ ...current, ...patch })); return <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 flex items-end justify-center bg-black/30 backdrop-blur-sm sm:items-center sm:p-6"><motion.form initial={{ y: 30 }} animate={{ y: 0 }} onSubmit={onSave} className="max-h-[92vh] w-full max-w-lg overflow-y-auto rounded-t-[2rem] bg-background p-6 shadow-2xl sm:rounded-[2rem]"><div className="mb-6 flex items-center justify-between"><h2 className="text-2xl font-semibold">{editing ? 'Edit objective' : 'Add a little win'}</h2><button type="button" onClick={onClose}><X /></button></div><div className="flex flex-col gap-4"><label>Objective name<input required value={form.title} onChange={event => update({ title: event.target.value })} className="mt-2 w-full rounded-xl border bg-background px-4 py-3" /></label><label>Icon<input value={form.icon} onChange={event => update({ icon: event.target.value })} className="mt-2 w-full rounded-xl border bg-background px-4 py-3" /></label><label>Category<select value={form.category} onChange={event => update({ category: event.target.value as Category })} className="mt-2 w-full rounded-xl border bg-background px-4 py-3">{['Health', 'Nutrition', 'Mind', 'Fitness', 'Relationships', 'Self-care', 'Learning', 'Custom'].map(category => <option key={category}>{category}</option>)}</select></label><label>Tracking type<select value={form.type} onChange={event => update({ type: event.target.value as ObjectiveType })} className="mt-2 w-full rounded-xl border bg-background px-4 py-3"><option value="boolean">Done / Not done</option><option value="counter">Counter</option><option value="duration">Duration</option><option value="quantity">Quantity</option></select></label>{form.type !== 'boolean' && <><label>Target<input type="number" min="1" value={form.target} onChange={event => update({ target: Number(event.target.value) })} className="mt-2 w-full rounded-xl border bg-background px-4 py-3" /></label><label>Unit<input value={form.unit} onChange={event => update({ unit: event.target.value })} className="mt-2 w-full rounded-xl border bg-background px-4 py-3" /></label></>}<label>Why does this matter to you?<textarea value={form.why} onChange={event => update({ why: event.target.value })} className="mt-2 min-h-24 w-full rounded-xl border bg-background px-4 py-3" /></label></div><div className="mt-7 flex gap-3"><button type="button" onClick={onClose} className="flex-1 rounded-xl border py-3">Cancel</button><button className="flex-1 rounded-xl bg-foreground py-3 font-semibold text-background">{editing ? 'Save changes' : 'Create objective'}</button></div></motion.form></motion.div> }
